export const disabledArgType = {
    table: {
        disable: true,
    },
};

export const defaultArgTypes = {
    id: disabledArgType,
    className: disabledArgType,
    onChange: disabledArgType,
    onClick: disabledArgType,
    onFocus: disabledArgType,
    onSubmit: disabledArgType,
    itemProp: disabledArgType,
    style: disabledArgType,
};
